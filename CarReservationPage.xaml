<UserControl x:Class="RIRentalCar.CarReservationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RIRentalCar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <!-- Lijevi stupac: Boxovi za automobile -->
        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Available Cars:" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox x:Name="CarListBox" SelectionChanged="CarListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Model}" FontWeight="Bold" />
                                <TextBlock Text="{Binding Year, StringFormat='Year: {0}'}" />
                                <TextBlock Text="{Binding PricePerDay, StringFormat='Price per day: {0:C}'}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <!-- Desni stupac: Odabir datuma i izračun cijene -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Choose dates:" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Start date:" Margin="0,0,0,5"/>
            <DatePicker x:Name="StartDatePicker" SelectedDateChanged="StartDatePicker_SelectedDateChanged" Margin="0,0,0,10"/>
            <TextBlock Text="End date:" Margin="0,0,0,5"/>
            <DatePicker x:Name="EndDatePicker" SelectedDateChanged="EndDatePicker_SelectedDateChanged" Margin="0,0,0,10"/>
            <TextBlock x:Name="TotalPriceTextBlock" Text="Total price: N/A" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Reserve" Click="ReserveButton_Click" Margin="5"/>
                <Button Content="Back to Main Menu" Click="BackToMainMenu_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
